name: 'Windows Desktop with RustDesk'

on:
  workflow_dispatch:
    inputs:
      rdp_password:
        description: 'RDP接続用のパスワード (runnerユーザー)'
        required: true
        type: string
      rustdesk_password:
        description: 'RustDesk接続用のパスワード'
        required: true
        type: string
      hold_time_minutes:
        description: 'デスクトップ環境を保持する時間 (分)'
        required: true
        default: '60'
        type: string

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: '1: RDPを有効化'
        shell: powershell
        run: |
          # ワークフロー入力からパスワードを取得
          $rdpPassword = "${{ github.event.inputs.rdp_password }}"
          
          # runnerユーザーのパスワードを設定
          net user runner $rdpPassword
          
          # レジストリを変更してRDPを有効化
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -value 0
          
          # ファイアウォールでRDPポートを許可
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
          # パスワードをログに出力しないようにマスク
          Write-Output "::add-mask::$rdpPassword"
          Write-Host "RDP has been enabled for user 'runner'."

      - name: '2: RustDeskをダウンロードして展開'
        shell: powershell
        run: |
          # RustDeskの最新ポータブル版をダウンロード (URLは将来変更される可能性があります)
          # 公式サイトやGitHub Releasesで最新のURLを確認してください
          $rustdeskUrl = "https://github.com/rustdesk/rustdesk/releases/download/1.2.3/rustdesk-1.2.3-x86_64-pc-windows-msvc.zip"
          $outputPath = "rustdesk.zip"
          
          Invoke-WebRequest -Uri $rustdeskUrl -OutFile $outputPath
          Expand-Archive -Path $outputPath -DestinationPath .\rustdesk
          Write-Host "RustDesk downloaded and extracted."

      - name: '3: RustDeskを起動して接続IDを取得'
        shell: powershell
        run: |
          $rustdeskPassword = "${{ github.event.inputs.rustdesk_password }}"
          
          # RustDeskをパスワード付きでバックグラウンドで起動
          Start-Process -FilePath ".\rustdesk\rustdesk.exe" -ArgumentList "--password", $rustdeskPassword
          
          # RustDeskが設定ファイルを生成するまで少し待機
          Start-Sleep -Seconds 10
          
          # 設定ファイルから接続IDを読み込む
          # 設定ファイルの場所: %APPDATA%\RustDesk\config\RustDesk.toml
          $configPath = "$env:APPDATA\RustDesk\config\RustDesk.toml"
          
          # ファイルが確実に生成されるまで待機（ループで確認）
          $timeout = 30
          $timer = 0
          while (-not (Test-Path $configPath) -and $timer -lt $timeout) {
            Start-Sleep -Seconds 1
            $timer++
          }

          if (Test-Path $configPath) {
            $configContent = Get-Content $configPath
            $idLine = $configContent | Where-Object { $_ -match '^\s*id\s*=\s*(\d+)' }
            if ($idLine) {
              $rustdeskId = $matches[1]
              Write-Output "rustdesk_id=$rustdeskId" >> $env:GITHUB_ENV
              Write-Host "RustDesk ID: $rustdeskId"
            } else {
              Write-Error "Could not find RustDesk ID in config file."
              exit 1
            }
          } else {
            Write-Error "RustDesk config file not found after waiting."
            exit 1
          }
          
          # パスワードをマスク
          Write-Output "::add-mask::$rustdeskPassword"
          Write-Host "RustDesk started with password."

      - name: '4: 接続情報をサマリーに表示'
        shell: powershell
        run: |
          # GitHubのUIで見やすいようにサマリーを作成
          $summary = @"
          # 🖥️ Remote Desktop Connection Details
          
          この仮想マシンは、指定された時間だけ保持されます。

          ## 📡 RustDesk (推奨)
          これがWeb経由で接続するための主要な方法です。

          - **RustDesk ID:** `${{ env.rustdesk_id }}`
          - **Password:** `${{ github.event.inputs.rustdesk_password }}`
          
          **接続手順:**
          1. お使いのPCに [RustDeskクライアント](https://rustdesk.com/) をインストールします。
          2. RustDeskを起動し、上記の「RustDesk ID」を入力して接続します。
          3. パスワードの入力が求められたら、上記の「Password」を入力します。

          ---

          ## 🖥️ RDP (参考情報)
          **注意:** GitHub ActionsのRunnerは一般的に外部からの直接RDP接続を許可していません。この情報は、同じ仮想ネットワーク内からアクセスする場合などに限られます。通常はRustDeskをご利用ください。

          - **Computer Name (Host):** `localhost` (Runner内でのみ有効)
          - **Username:** `runner`
          - **Password:** `${{ github.event.inputs.rustdesk_password }}`
          "@
          
          # サマリーに出力
          echo $summary >> $env:GITHUB_STEP_SUMMARY

      - name: '5: 指定時間、ジョブを保持'
        shell: powershell
        run: |
          $holdMinutes = [int]"${{ github.event.inputs.hold_time_minutes }}"
          $holdSeconds = $holdMinutes * 60
          Write-Host "Holding the desktop session for $holdMinutes minutes ($holdSeconds seconds)."
          Write-Host "The workflow will automatically end after this time."
          # 指定された時間だけスリープしてジョブを維持する
          Start-Sleep -Seconds $holdSeconds
